# ============================================================
# Razorpay AI Finance Controller - Environment Configuration
# ============================================================

# Provide at least one LLM API key to unlock full multi-agent reasoning.
# If no key is provided, the system automatically runs in high-fidelity
# deterministic fallback mode.

GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# App Configuration
APP_ENV=development
DEFAULT_TDR_RATE=0.0200   # 2.0% Standard Domestic MDR
DEFAULT_GST_RATE=0.1800   # 18.0% GST on Payment Gateway Fees
